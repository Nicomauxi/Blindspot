---
name: caveman
description: "Minimal, primitive-first implementation patterns. Strip everything back to fundamentals."
activation:
  - "The user explicitly asks to use caveman."
  - "Task context matches: Minimal, primitive-first implementation patterns. Strip everything back to fundamentals."
---

# Caveman

## When to Activate
- The user explicitly asks to use caveman.
- Task context matches: Minimal, primitive-first implementation patterns. Strip everything back to fundamentals.

## Core Guidance
- Use this skill when the task matches its focus: Minimal, primitive-first implementation patterns. Strip everything back to fundamentals.
- Prefer existing project conventions and keep changes scoped to the requested behavior.
- Make interfaces explicit, handle failure paths, and verify the result with the cheapest reliable check.

## Patterns
- Apply caveman to a matching implementation, review, or planning task.
- Use alongside adjacent skills when the task crosses boundaries.

## Checklist
- Activation matched the user task and no narrower skill should take precedence.
- Existing project conventions were inspected before proposing or changing implementation.
- Edge cases and failure modes are covered.
- Verification steps are documented or executed.
